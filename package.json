{
  "scripts": {
    "format": "prettier --write \"**/*.sql\"",
    "format:check": "prettier --check \"**/*.sql\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "3.3.2",
    "prettier-plugin-sql": "^0.18.0"
  }
}
